# loopback_users.guest = false
loopback_users = none
listeners.ssl.default = 5671
ssl_options.cacertfile = /certs/testca/ca_certificate.pem
ssl_options.certfile = /certs/server/rabbitmq_combined.pem
ssl_options.fail_if_no_peer_cert = false
ssl_options.keyfile = /certs/server/rabbitmq.key
ssl_options.verify = verify_none
# default_pass = guest
# default_user = guest
# consumer_timeout = 60000
message_interceptors.incoming.set_header_timestamp.overwrite = true
# Reduce the management stats collection interval so that new connections
# appear in /api/connections quickly.  RabbitMQ flushes connection state to
# its stats DB on this cycle, so a freshly reconnected client may not be
# visible until the next flush.  500 ms gives worst-case ~500 ms API lag
# (down from the 5 s default), keeping failover-test detection well under 2 s.
collect_statistics_interval = 500

# uncomment for non-tls testing, comment out for tls testing
management.tcp.port = 15672

# uncomment for tls testing, comment out for non-tls testing
# management.ssl.port = 15671
# management.ssl.cacertfile = /certs/testca/ca_certificate.pem
# management.ssl.certfile = /certs/server/rabbitmq_combined.pem
# management.ssl.keyfile = /certs/server/rabbitmq.key
# management.ssl.verify = verify_none
# stream.listeners.ssl.1 = 5552
# stream.listeners.tcp = none